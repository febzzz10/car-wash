{
  "name": "@washpro/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --env production --outdir dist",
    "deploy": "wrangler deploy --env production",
    "deploy:production": "wrangler deploy --env production",
    "dev": "wrangler dev --env=\"\"",
    "dev:remote": "wrangler dev --env remote-dev",
    "predeploy": "node scripts/validate-production-deploy.mjs",
    "db:migrate": "wrangler d1 migrations apply washpro-dev --remote",
    "db:migrate:production": "wrangler d1 migrations apply washpro-dev --remote --env production",
    "db:migrate:remote-dev": "wrangler d1 migrations apply washpro-dev --remote --env remote-dev",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@washpro/contracts": "1.0.0",
    "@washpro/domain": "1.0.0",
    "hono": "^4.12.31",
    "pdf-lib": "^1.17.1"
  }
}
